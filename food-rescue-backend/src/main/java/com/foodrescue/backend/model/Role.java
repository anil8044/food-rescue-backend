package com.foodrescue.backend.model;

/**
 * The three user roles supported by the platform (see Assessment 1 proposal,
 * Roles and Responsibilities / Scope sections).
 */
public enum Role {
    DONOR,
    RECIPIENT_ORG,
    ADMIN
}
